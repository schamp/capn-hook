{
  "name": "capn-hook",
  "version": "0.0.0",
  "description": "Doesn't really use node, but uses the git-hooks tool as an example to run the tests.",
  "scripts": {
    "test": ".githooks/test/pre-commit.test.sh"
  },
  "author": "Andrew Schamp",
  "license": "MIT",
  "dependencies": {
  },
  "devDependencies": {

    "git-hooks": "^1.1.0"
  }
}
